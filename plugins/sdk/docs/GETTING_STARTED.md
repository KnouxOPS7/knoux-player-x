# Getting Started with Knoux Plugins

1. Install the CLI: `npm i -g @knoux/cli`
2. Create project: `knoux-cli create my-plugin`
3. Develop: Edit `index.ts`
4. Build: `npm run build`
5. Test: Copy `dist/` to `%APPDATA%/KnouxPlayerX/plugins/`
